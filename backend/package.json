{
  "name": "nothing-ever-happens-backend",
  "version": "1.0.0",
  "description": "Backend firestore rules and tests",
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"npm run test:inner\"",
    "test:inner": "mocha test/**/*.spec.js --timeout 10000"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase-tools": "^15.20.0",
    "mocha": "^11.7.6"
  },
  "overrides": {
    "universal-analytics": "0.5.3"
  }
}
