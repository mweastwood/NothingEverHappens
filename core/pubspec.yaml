name: core
description: Shared domain models and scheduling engine for Nothing Ever Happens.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  collection: ^1.18.0
  meta: ^1.11.0
  uuid: ^4.5.1

dev_dependencies:
  test: ^1.24.0
